export interface SuccessMessage {
   success: {
        title: string
        explaination: string
        url: string
        buttontitle: string
    }
}

export interface SuccessContent {
    success: {
         title: string
         explaination: string
     }
 }

 export interface SuccessAction {
     url:string
     buttontitle: string
 }